{
  "title": "Behavior Trees",
  "slug": "guides/behavior-trees",
  "section": "Getting Started",
  "order": 3,
  "navTitle": "Behavior Trees",
  "description": "Understand how to design and implement AI behavior trees in the Blastback Engine.",
  "keywords": ["behavior tree", "AI", "nodes", "commands", "decorator", "sequence", "selector", "patrol", "chase", "resolution"],
  "content": "<h1 id=\"behavior-trees\">Behavior Trees</h1>\n<p class=\"page-subtitle\">Design intelligent AI behaviors using the Blastback Engine's data-driven behavior tree system. Define complex decision-making logic entirely in JSON.</p>\n\n<h2 id=\"what-are-behavior-trees\">What Are Behavior Trees?</h2>\n<p>Behavior trees (BTs) are a hierarchical model for organizing AI decision-making. Unlike finite state machines, behavior trees offer a modular, composable structure that scales gracefully from simple patrol routines to elaborate boss fight sequences.</p>\n<p>Blastback Engine uses behavior trees as the primary AI system because they provide:</p>\n<ul>\n<li><strong>Modularity</strong> &mdash; individual sub-trees can be reused across many entity types.</li>\n<li><strong>Readability</strong> &mdash; the tree structure maps directly to how designers think about AI priorities.</li>\n<li><strong>Data-driven authoring</strong> &mdash; trees are plain JSON files, editable by hand or with the visual editor.</li>\n<li><strong>Runtime flexibility</strong> &mdash; the resolution system lets entities override specific nodes without duplicating the whole tree.</li>\n</ul>\n\n<div class=\"callout info\"><div class=\"callout-title\">Note</div>Every tick, the behavior tree is evaluated from the root. Each node returns one of three statuses: <strong>Success</strong>, <strong>Failure</strong>, or <strong>Running</strong>. The parent node uses these statuses to decide which child to evaluate next.</div>\n\n<h2 id=\"bt-file-convention\">Creating Behavior Tree Files</h2>\n<p>Behavior tree files follow a strict naming convention so the engine can discover and load them automatically:</p>\n<pre><code>bt.[ID].json</code></pre>\n<p>The <code>[ID]</code> portion becomes the identifier you reference from an entity's <code>ai</code> component via <code>templateID</code>.</p>\n\n<div class=\"code-header\"><span>Example file names</span></div>\n<pre><code>bt.basic_chase.json\nbt.patrol_and_attack.json\nbt.boss_phase1.json</code></pre>\n\n<div class=\"callout warning\"><div class=\"callout-title\">Important</div>The <code>bt.</code> prefix is mandatory. Without it, the engine will not recognize the file as a behavior tree definition.</div>\n\n<h2 id=\"node-structure\">BT Node Structure</h2>\n<p>Every node in a Blastback behavior tree is a JSON object with a specific set of fields. The node's role in the tree is determined by its type, which is encoded in the key name when the node appears as a child.</p>\n\n<h3 id=\"node-schema\">Node JSON Schema</h3>\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td><code>commands</code></td><td>string[]</td><td>No</td><td>List of command strings executed when the node is reached. Commands that perform checks return Success or Failure.</td></tr>\n<tr><td><code>children</code></td><td>object</td><td>No</td><td>Dictionary of child nodes. Keys use the format <code>type.name</code> to encode the child's node type and identifier.</td></tr>\n<tr><td><code>parameters</code></td><td>object</td><td>No</td><td>Configuration parameters for decorator nodes (e.g., repeat count, cooldown time).</td></tr>\n</tbody>\n</table></div>\n\n<div class=\"code-header\"><span>Basic node structure</span></div>\n<pre><code>{\n  \"commands\": [\"ai.checkPlayerInRange 120\"],\n  \"children\": {\n    \"action.chasePlayer\": {\n      \"commands\": [\"ai.moveToTarget player\"]\n    },\n    \"action.idle\": {\n      \"commands\": [\"entity.animate idle,loop\"]\n    }\n  }\n}</code></pre>\n\n<h2 id=\"node-types\">Node Types</h2>\n<p>The node type is identified by the key prefix in the <code>children</code> dictionary. The format is <code>type.name</code>, where <code>type</code> determines the node's evaluation behavior and <code>name</code> is a unique identifier.</p>\n\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Type Prefix</th><th>Node Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td><code>sequence</code></td><td>Sequence</td><td>Runs children left to right. Stops and returns Failure if any child fails. Returns Success only if all children succeed.</td></tr>\n<tr><td><code>selector</code></td><td>Selector</td><td>Runs children left to right. Stops and returns Success if any child succeeds. Returns Failure only if all children fail.</td></tr>\n<tr><td><code>action</code></td><td>Action</td><td>A leaf node that executes its commands. Returns the result of the last command.</td></tr>\n<tr><td><code>decorator</code></td><td>Decorator</td><td>Wraps a single child and modifies its result based on <code>parameters</code> (e.g., invert, repeat, cooldown).</td></tr>\n<tr><td><code>condition</code></td><td>Condition</td><td>Evaluates a check command. Returns Success if the condition is met, Failure otherwise. Does not execute side-effect actions.</td></tr>\n</tbody>\n</table></div>\n\n<h3 id=\"node-identification\">Node Type Identification via Children Key</h3>\n<p>The <code>children</code> key in a node is a dictionary where each key follows the <code>type.name</code> pattern. The engine parses the prefix before the first dot to determine the node type, and uses the portion after the dot as the node's unique identifier within the tree.</p>\n\n<div class=\"code-header\"><span>Node type identification examples</span></div>\n<pre><code>\"children\": {\n  \"selector.mainBehavior\": { ... },\n  \"sequence.attackRoutine\": { ... },\n  \"action.flee\": { ... },\n  \"decorator.repeatForever\": { ... },\n  \"condition.isPlayerClose\": { ... }\n}</code></pre>\n\n<div class=\"callout info\"><div class=\"callout-title\">Note</div>The name portion of the key (after the dot) is used as the <code>nodeID</code> for AI events and for the resolution system. Choose descriptive, unique names so you can easily target specific nodes for overrides and event listeners.</div>\n\n<h2 id=\"commands-in-nodes\">Commands in Nodes</h2>\n<p>The <code>commands</code> array contains command strings that are executed when the node is evaluated. Commands serve two purposes:</p>\n<ol>\n<li><strong>Condition checks</strong> &mdash; commands that test a condition and return Success or Failure, controlling tree traversal.</li>\n<li><strong>Actions</strong> &mdash; commands that perform side effects (move, animate, attack) and return a status.</li>\n</ol>\n\n<p>When a node has multiple commands, they are executed in order. If any command returns Failure, the node immediately returns Failure without executing the remaining commands.</p>\n\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Command</th><th>Type</th><th>Description</th><th>Returns</th></tr></thead>\n<tbody>\n<tr><td><code>ai.checkPlayerInRange &lt;distance&gt;</code></td><td>Condition</td><td>Checks if the player is within the specified pixel distance.</td><td>Success / Failure</td></tr>\n<tr><td><code>ai.moveToTarget &lt;targetType&gt;</code></td><td>Action</td><td>Moves the entity toward the specified target.</td><td>Running / Success</td></tr>\n<tr><td><code>ai.wait &lt;seconds&gt;</code></td><td>Action</td><td>Waits for the specified duration.</td><td>Running / Success</td></tr>\n<tr><td><code>ai.setTarget &lt;targetType&gt;</code></td><td>Action</td><td>Sets the current AI target for subsequent commands.</td><td>Success</td></tr>\n<tr><td><code>ai.checkHealth &lt;threshold&gt;</code></td><td>Condition</td><td>Checks if entity health is above the threshold.</td><td>Success / Failure</td></tr>\n<tr><td><code>ai.patrol &lt;distance&gt;</code></td><td>Action</td><td>Patrols back and forth within the given distance from the spawn point.</td><td>Running</td></tr>\n<tr><td><code>ai.flee &lt;distance&gt;</code></td><td>Action</td><td>Moves the entity away from the current target.</td><td>Running / Success</td></tr>\n<tr><td><code>entity.emit &lt;eventID&gt;</code></td><td>Action</td><td>Emits an event on the entity.</td><td>Success</td></tr>\n<tr><td><code>entity.animate &lt;anim&gt;,&lt;mode&gt;</code></td><td>Action</td><td>Plays an animation on the entity.</td><td>Success</td></tr>\n</tbody>\n</table></div>\n\n<div class=\"callout tip\"><div class=\"callout-title\">Tip</div>You can chain a condition check before an action in the same node's <code>commands</code> array. If the condition fails, the action is never executed, and the node returns Failure immediately. This is a concise alternative to using a separate <code>condition</code> child node.</div>\n\n<h2 id=\"parameters-decorators\">Parameters for Decorator Nodes</h2>\n<p>Decorator nodes use the <code>parameters</code> object to configure their wrapping behavior. The available parameters depend on the decorator subtype.</p>\n\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td><code>repeatCount</code></td><td>int</td><td>Number of times to repeat the child. Use <code>-1</code> for infinite repetition.</td></tr>\n<tr><td><code>cooldown</code></td><td>float</td><td>Minimum seconds between executions of the child node.</td></tr>\n<tr><td><code>invert</code></td><td>bool</td><td>If true, inverts the child's Success/Failure result.</td></tr>\n<tr><td><code>succeedAlways</code></td><td>bool</td><td>If true, always returns Success regardless of the child's result.</td></tr>\n</tbody>\n</table></div>\n\n<div class=\"code-header\"><span>Decorator with parameters</span></div>\n<pre><code>\"decorator.repeatPatrol\": {\n  \"parameters\": {\n    \"repeatCount\": -1\n  },\n  \"children\": {\n    \"action.patrolStep\": {\n      \"commands\": [\"ai.patrol 80\"]\n    }\n  }\n}</code></pre>\n\n<div class=\"code-header\"><span>Cooldown decorator example</span></div>\n<pre><code>\"decorator.attackCooldown\": {\n  \"parameters\": {\n    \"cooldown\": 1.5\n  },\n  \"children\": {\n    \"action.attack\": {\n      \"commands\": [\n        \"entity.animate attack,once\",\n        \"entity.emit meleeAttack\"\n      ]\n    }\n  }\n}</code></pre>\n\n<h2 id=\"inline-vs-referenced\">Inline Templates vs. Referenced templateID</h2>\n<p>There are two ways to assign a behavior tree to an entity's <code>ai</code> component:</p>\n\n<h3 id=\"referenced-template\">Referenced Template (Recommended)</h3>\n<p>Point to an external <code>bt.[ID].json</code> file using <code>templateID</code>. This is the preferred approach for reusable behaviors shared across multiple entities.</p>\n\n<div class=\"code-header\"><span>Entity AI component with templateID</span></div>\n<pre><code>\"ai\": {\n  \"templateID\": \"patrol_and_attack\"\n}</code></pre>\n<p>The engine will look for a file named <code>bt.patrol_and_attack.json</code> and load its tree definition.</p>\n\n<h3 id=\"inline-template\">Inline Template</h3>\n<p>Define the tree directly inside the entity JSON. Useful for one-off behaviors unique to a single entity.</p>\n\n<div class=\"code-header\"><span>Entity AI component with inline tree</span></div>\n<pre><code>\"ai\": {\n  \"tree\": {\n    \"children\": {\n      \"selector.root\": {\n        \"children\": {\n          \"sequence.chase\": {\n            \"children\": {\n              \"condition.playerNearby\": {\n                \"commands\": [\"ai.checkPlayerInRange 80\"]\n              },\n              \"action.pursue\": {\n                \"commands\": [\"ai.moveToTarget player\"]\n              }\n            }\n          },\n          \"action.idle\": {\n            \"commands\": [\"entity.animate idle,loop\"]\n          }\n        }\n      }\n    }\n  }\n}</code></pre>\n\n<div class=\"callout info\"><div class=\"callout-title\">Note</div>Inline templates cannot be shared across entities. If you find yourself duplicating an inline tree, extract it into a <code>bt.[ID].json</code> file and use <code>templateID</code> instead.</div>\n\n<h2 id=\"resolution-system\">The Resolution System</h2>\n<p>The resolution system is the key to creating modular, reusable AI. It allows entities to override specific nodes in a referenced behavior tree without duplicating the entire tree.</p>\n<p>When an entity references a <code>templateID</code>, it can provide a <code>resolution</code> object that maps node names to replacement definitions. The engine merges the resolution into the base tree at load time.</p>\n\n<div class=\"code-header\"><span>Entity with resolution overrides</span></div>\n<pre><code>\"ai\": {\n  \"templateID\": \"patrol_and_attack\",\n  \"resolution\": {\n    \"attackAction\": {\n      \"commands\": [\n        \"entity.animate slash,once\",\n        \"entity.emit meleeAttack\"\n      ]\n    },\n    \"patrolStep\": {\n      \"commands\": [\"ai.patrol 120\"]\n    }\n  }\n}</code></pre>\n\n<p>In the base tree <code>bt.patrol_and_attack.json</code>, any node whose name portion (the part after the dot in <code>type.name</code>) matches a key in <code>resolution</code> will have its <code>commands</code> (and optionally <code>children</code>) replaced by the resolution entry.</p>\n\n<div class=\"callout tip\"><div class=\"callout-title\">Tip</div>Design your base behavior trees with clearly named &quot;hook&quot; nodes that you expect entities to override. For example, name your attack node <code>action.attackAction</code> so every entity can provide its own attack commands via resolution without touching the tree structure.</div>\n\n<h3 id=\"resolution-example\">Resolution in Practice</h3>\n<p>Consider a base tree used by all melee enemies. Two different enemies override it differently:</p>\n\n<div class=\"code-header\"><span>Goblin uses a sword slash</span></div>\n<pre><code>\"ai\": {\n  \"templateID\": \"melee_enemy\",\n  \"resolution\": {\n    \"attackAction\": {\n      \"commands\": [\"entity.animate sword_slash,once\"]\n    }\n  }\n}</code></pre>\n\n<div class=\"code-header\"><span>Troll uses a ground pound</span></div>\n<pre><code>\"ai\": {\n  \"templateID\": \"melee_enemy\",\n  \"resolution\": {\n    \"attackAction\": {\n      \"commands\": [\n        \"entity.animate ground_pound,once\",\n        \"entity.emit aoeAttack\"\n      ]\n    }\n  }\n}</code></pre>\n\n<h2 id=\"ai-events\">AI Component Events</h2>\n<p>The AI system emits events when specific nodes start and end execution. These events follow the naming convention:</p>\n<pre><code>ai.{nodeID}.onStart\nai.{nodeID}.onEnd</code></pre>\n<p>Where <code>{nodeID}</code> is the name portion of the node key (e.g., for <code>action.chasePlayer</code>, the nodeID is <code>chasePlayer</code>).</p>\n\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Event Pattern</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td><code>ai.{nodeID}.onStart</code></td><td>Fires when the node begins execution for the first time in a traversal.</td></tr>\n<tr><td><code>ai.{nodeID}.onEnd</code></td><td>Fires when the node completes execution (returns Success or Failure, not Running).</td></tr>\n</tbody>\n</table></div>\n\n<p>These events can be captured by the entity's <code>commandMap</code> or model events to trigger animations, sounds, or other effects synchronized with AI state changes.</p>\n\n<div class=\"code-header\"><span>Reacting to AI events in an entity model</span></div>\n<pre><code>\"model\": {\n  \"events\": {\n    \"ai.chasePlayer.onStart\": {\n      \"playSound\": { \"id\": \"enemy_alert\" }\n    },\n    \"ai.chasePlayer.onEnd\": {\n      \"playSound\": { \"id\": \"enemy_lost_target\" }\n    },\n    \"ai.attackAction.onStart\": {\n      \"spawnEffect\": { \"id\": \"attack_telegraph\" }\n    }\n  }\n}</code></pre>\n\n<div class=\"code-header\"><span>Reacting to AI events via commandMap</span></div>\n<pre><code>\"components\": {\n  \"mob\": {\n    \"commandMap\": {\n      \"ai.chasePlayer.onStart\": \"entity.animate run,loop\",\n      \"ai.patrolStep.onStart\": \"entity.animate walk,loop\",\n      \"ai.idle.onStart\": \"entity.animate idle,loop\"\n    }\n  }\n}</code></pre>\n\n<h2 id=\"online-editor\">Online AI Editor</h2>\n<p>Blastback provides a visual behavior tree editor that lets you design, test, and export trees without editing JSON by hand.</p>\n<p><a href=\"https://jaschez.github.io/blastback/ai-editor/\" target=\"_blank\">Open the Online AI Editor</a></p>\n<p>The editor supports:</p>\n<ul>\n<li>Drag-and-drop node placement and rearrangement</li>\n<li>Real-time tree validation and error highlighting</li>\n<li>Export directly to <code>bt.[ID].json</code> format</li>\n<li>Import existing trees for editing and iteration</li>\n<li>Visual debugging with simulated tick execution</li>\n<li>Node search and filtering for large trees</li>\n</ul>\n\n<div class=\"callout tip\"><div class=\"callout-title\">Tip</div>The online editor is the fastest way to prototype complex behavior trees. Design your tree visually, export it, and then fine-tune the JSON if needed.</div>\n\n<h2 id=\"complete-example\">Complete Example: Patrol and Chase</h2>\n<p>Here is a complete behavior tree that implements a patrol-and-chase pattern. The entity patrols a fixed area, and when the player comes within range, it switches to chasing and attacking.</p>\n\n<div class=\"code-header\"><span>bt.patrol_and_chase.json</span></div>\n<pre><code>{\n  \"children\": {\n    \"selector.root\": {\n      \"children\": {\n        \"sequence.chaseAndAttack\": {\n          \"children\": {\n            \"condition.playerInRange\": {\n              \"commands\": [\"ai.checkPlayerInRange 100\"]\n            },\n            \"action.setPlayerTarget\": {\n              \"commands\": [\"ai.setTarget player\"]\n            },\n            \"selector.engageBehavior\": {\n              \"children\": {\n                \"sequence.attackIfClose\": {\n                  \"children\": {\n                    \"condition.inAttackRange\": {\n                      \"commands\": [\"ai.checkPlayerInRange 24\"]\n                    },\n                    \"action.attackAction\": {\n                      \"commands\": [\n                        \"entity.animate attack,once\",\n                        \"entity.emit meleeAttack\"\n                      ]\n                    },\n                    \"action.attackCooldown\": {\n                      \"commands\": [\"ai.wait 0.8\"]\n                    }\n                  }\n                },\n                \"action.chasePlayer\": {\n                  \"commands\": [\n                    \"entity.animate walk,loop\",\n                    \"ai.moveToTarget player\"\n                  ]\n                }\n              }\n            }\n          }\n        },\n        \"decorator.repeatPatrol\": {\n          \"parameters\": {\n            \"repeatCount\": -1\n          },\n          \"children\": {\n            \"sequence.patrolLoop\": {\n              \"children\": {\n                \"action.patrolStep\": {\n                  \"commands\": [\n                    \"entity.animate walk,loop\",\n                    \"ai.patrol 80\"\n                  ]\n                },\n                \"action.patrolPause\": {\n                  \"commands\": [\n                    \"entity.animate idle,loop\",\n                    \"ai.wait 2.0\"\n                  ]\n                }\n              }\n            }\n          }\n        }\n      }\n    }\n  }\n}</code></pre>\n\n<h3 id=\"example-breakdown\">How This Tree Works</h3>\n<ol>\n<li>The root <strong>selector</strong> tries the chase-and-attack sequence first.</li>\n<li>The <code>condition.playerInRange</code> node checks if the player is within 100 pixels. If the check <strong>fails</strong>, the entire <code>sequence.chaseAndAttack</code> fails, and the selector moves to the patrol branch.</li>\n<li>If the player is in range, the entity sets the player as its target and enters the engage selector.</li>\n<li>The engage selector first tries to attack (if the player is within 24 pixels), then falls back to chasing.</li>\n<li>If no player is nearby, the patrol decorator repeats indefinitely: walk 80 pixels, pause for 2 seconds, then repeat.</li>\n</ol>\n\n<h3 id=\"using-with-entity\">Using This Tree with an Entity</h3>\n<div class=\"code-header\"><span>enemy_guard.mob.json (excerpt)</span></div>\n<pre><code>\"ai\": {\n  \"templateID\": \"patrol_and_chase\",\n  \"resolution\": {\n    \"attackAction\": {\n      \"commands\": [\n        \"entity.animate spear_thrust,once\",\n        \"entity.emit meleeAttack\"\n      ]\n    }\n  }\n}</code></pre>\n<p>This guard entity uses the <code>patrol_and_chase</code> tree but overrides the <code>attackAction</code> node with a custom spear thrust animation via the resolution system.</p>\n\n<div class=\"callout info\"><div class=\"callout-title\">Next Steps</div>Now that you understand behavior trees, see the <a href=\"/guides/entity-scripting\">Entity Scripting</a> guide to learn how to wire AI components into full entity definitions, or explore the <a href=\"/guides/modding-guide\">Modding Guide</a> for end-to-end examples.</div>"
}
