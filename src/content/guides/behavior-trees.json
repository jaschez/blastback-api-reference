{
  "title": "Behavior Trees Guide",
  "slug": "guides/behavior-trees",
  "section": "Getting Started",
  "subsection": null,
  "order": 3,
  "navTitle": "Behavior Trees",
  "description": "Learn how to create AI behavior trees for Blastback Engine entities, including node types, commands, templates, the resolution system, and the online AI editor.",
  "keywords": ["behavior trees", "AI", "nodes", "commands", "templates", "bt", "patrol", "chase", "AI editor"],
  "showToc": true,
  "content": "<h1>Behavior Trees Guide</h1><p>Behavior trees (BTs) are the AI system used by Blastback Engine to drive entity decision-making. They provide a structured, modular way to define how enemies patrol, detect players, attack, retreat, and perform other autonomous behaviors.</p><h2 id=\"what-are-behavior-trees\">What Are Behavior Trees?</h2><p>A behavior tree is a hierarchical structure of <strong>nodes</strong> that are evaluated each frame. Starting from the root, the tree traverses its children in a defined order, executing commands and checking conditions along the way. Each node returns one of three states:</p><ul><li><strong>Success</strong> -- the node completed its task</li><li><strong>Failure</strong> -- the node could not complete its task</li><li><strong>Running</strong> -- the node is still in progress</li></ul><p>This simple model enables surprisingly complex behaviors when nodes are composed together. Blastback uses behavior trees because they are:</p><ul><li><strong>Visual and debuggable</strong> -- the tree structure makes it easy to trace decisions</li><li><strong>Modular</strong> -- subtrees can be reused across different entities via templates</li><li><strong>Data-driven</strong> -- trees are defined in JSON, no Lua code required for standard AI patterns</li></ul><h2 id=\"node-structure\">BT Node Structure</h2><p>Every node in a behavior tree has the following structure:</p><div class=\"code-header\"><span>JSON</span></div><pre><code>{\n  \"type\": \"sequence\",\n  \"commands\": [\n    {\n      \"name\": \"ai.checkDistance\",\n      \"parameters\": {\n        \"target\": \"player\",\n        \"maxDistance\": 120\n      }\n    }\n  ],\n  \"children\": {\n    \"moveToTarget\": {\n      \"type\": \"action\",\n      \"commands\": [\n        {\n          \"name\": \"ai.moveTo\",\n          \"parameters\": {\n            \"target\": \"player\",\n            \"speed\": 60\n          }\n        }\n      ]\n    }\n  }\n}</code></pre><h3>Node Properties</h3><div class=\"table-wrapper\"><table><thead><tr><th>Property</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>type</code></td><td>string</td><td>The node type (sequence, selector, action, decorator, etc.)</td></tr><tr><td><code>commands</code></td><td>array</td><td>Commands to execute when this node is evaluated</td></tr><tr><td><code>children</code></td><td>object</td><td>Child nodes, keyed by their ID</td></tr><tr><td><code>parameters</code></td><td>object</td><td>Optional parameters specific to the node type</td></tr></tbody></table></div><h3>Core Node Types</h3><div class=\"table-wrapper\"><table><thead><tr><th>Type</th><th>Behavior</th></tr></thead><tbody><tr><td><code>sequence</code></td><td>Runs children in order. Fails if any child fails. Succeeds when all children succeed.</td></tr><tr><td><code>selector</code></td><td>Runs children in order. Succeeds if any child succeeds. Fails when all children fail.</td></tr><tr><td><code>action</code></td><td>A leaf node that executes one or more commands.</td></tr><tr><td><code>decorator</code></td><td>Wraps a single child and modifies its result (invert, repeat, etc.).</td></tr><tr><td><code>parallel</code></td><td>Runs all children simultaneously.</td></tr></tbody></table></div><h2 id=\"creating-bt-files\">Creating BT Files</h2><p>Behavior tree files must follow the naming convention <code>bt.[ID].json</code> and be placed in the <code>Content/BehaviorTrees/</code> directory. The ID is what you reference from the AI component on an entity.</p><div class=\"code-header\"><span>JSON</span></div><pre><code>// File: bt.slime_patrol.json\n{\n  \"root\": {\n    \"type\": \"selector\",\n    \"children\": {\n      \"chase\": {\n        \"type\": \"sequence\",\n        \"commands\": [\n          {\n            \"name\": \"ai.checkDistance\",\n            \"parameters\": {\n              \"target\": \"player\",\n              \"maxDistance\": 80\n            }\n          }\n        ],\n        \"children\": {\n          \"pursue\": {\n            \"type\": \"action\",\n            \"commands\": [\n              {\n                \"name\": \"ai.moveTo\",\n                \"parameters\": { \"target\": \"player\" }\n              }\n            ]\n          }\n        }\n      },\n      \"patrol\": {\n        \"type\": \"action\",\n        \"commands\": [\n          {\n            \"name\": \"ai.patrol\",\n            \"parameters\": {\n              \"radius\": 40,\n              \"waitTime\": 2.0\n            }\n          }\n        ]\n      }\n    }\n  }\n}</code></pre><div class=\"callout tip\"><div class=\"callout-title\">Tip</div>The AI component references the behavior tree by its ID (the part after <code>bt.</code>). For a file named <code>bt.slime_patrol.json</code>, set the component parameter to <code>\"behaviorTree\": \"slime_patrol\"</code>.</div><h2 id=\"node-identification\">Node Type Identification System</h2><p>In the children object, each key represents the node's ID. The node type can be identified by examining the <code>type</code> property within the child object. The key format <code>\"type.name\"</code> can also be used as a shorthand to identify nodes:</p><div class=\"code-header\"><span>JSON</span></div><pre><code>\"children\": {\n  \"type.name\": {\n    \"type\": \"sequence\",\n    \"commands\": [...]\n  }\n}</code></pre><p>This identification system allows the engine and AI editor to display and manipulate nodes by both their functional type and their semantic name within the tree.</p><h2 id=\"templates\">Inline Templates vs Referenced Templates</h2><p>Behavior trees support two template approaches for reusing subtrees across multiple entities:</p><h3>Inline Templates</h3><p>Define the full subtree directly inside the behavior tree file. This is the simplest approach and works well for behaviors unique to a single entity type.</p><h3>Referenced Templates (templateID)</h3><p>Reference a shared subtree by its template ID. This keeps your BT files DRY and makes it easy to update shared behaviors in one place.</p><div class=\"code-header\"><span>JSON</span></div><pre><code>{\n  \"chase\": {\n    \"templateID\": \"common_chase\",\n    \"parameters\": {\n      \"speed\": 80,\n      \"detectionRange\": 100\n    }\n  }\n}</code></pre><p>When the engine encounters a <code>templateID</code>, it looks up the referenced template and injects it into the tree with the provided parameter overrides. This is particularly useful for common patterns like chasing, fleeing, or patrolling.</p><h2 id=\"resolution-system\">Resolution System</h2><p>The resolution system allows behavior trees to be built modularly. When a BT file is loaded, the engine resolves all template references, merges parameters, and produces a flattened tree structure. This means:</p><ul><li>Templates can reference other templates (nested resolution)</li><li>Parameters cascade from parent to child, with child values taking priority</li><li>The final resolved tree is cached for performance</li></ul><div class=\"callout info\"><div class=\"callout-title\">Note</div>Resolution happens at load time, not at runtime. Once a behavior tree is loaded and resolved, it runs with zero overhead from the template system.</div><h2 id=\"commands-in-nodes\">Commands in Nodes</h2><p>Each node can have an array of commands that are executed when the node is evaluated. Commands in BT nodes serve dual purposes:</p><ol><li><strong>Condition checking</strong> -- commands can return success or failure to control tree flow</li><li><strong>Action execution</strong> -- commands can trigger entity behavior</li></ol><div class=\"code-header\"><span>JSON</span></div><pre><code>\"commands\": [\n  {\n    \"name\": \"ai.checkDistance\",\n    \"parameters\": {\n      \"target\": \"player\",\n      \"maxDistance\": 100\n    }\n  },\n  {\n    \"name\": \"ai.faceTarget\",\n    \"parameters\": {\n      \"target\": \"player\"\n    }\n  }\n]</code></pre><p>Commands are evaluated in order. If a command returns failure, the node itself fails (for sequence-type behavior). This allows you to use commands as both guards and actions within the same node.</p><h3>Common BT Commands</h3><div class=\"table-wrapper\"><table><thead><tr><th>Command</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>ai.checkDistance</code></td><td>Condition</td><td>Checks if target is within a specified distance</td></tr><tr><td><code>ai.checkHealth</code></td><td>Condition</td><td>Checks if entity health is above/below a threshold</td></tr><tr><td><code>ai.moveTo</code></td><td>Action</td><td>Moves entity toward a target</td></tr><tr><td><code>ai.patrol</code></td><td>Action</td><td>Moves entity between random points in a radius</td></tr><tr><td><code>ai.faceTarget</code></td><td>Action</td><td>Rotates entity to face the target</td></tr><tr><td><code>ai.attack</code></td><td>Action</td><td>Triggers the entity's weapon or attack command</td></tr><tr><td><code>ai.wait</code></td><td>Action</td><td>Pauses the node for a specified duration</td></tr><tr><td><code>ai.flee</code></td><td>Action</td><td>Moves entity away from the target</td></tr></tbody></table></div><h2 id=\"ai-editor\">Online AI Editor</h2><p>Blastback Engine provides a visual browser-based tool for designing and testing behavior trees without manually editing JSON files.</p><p><a href=\"https://jaschez.github.io/blastback/ai-editor/\" class=\"button\">Open AI Editor</a></p><p>The AI editor allows you to:</p><ul><li>Visually construct behavior trees with drag-and-drop nodes</li><li>Configure node commands and parameters through a form interface</li><li>Export trees as properly formatted <code>bt.[ID].json</code> files</li><li>Import existing trees for editing</li><li>Test and validate tree structure before using it in-game</li></ul><div class=\"callout tip\"><div class=\"callout-title\">Tip</div>The AI editor is the recommended way to create and edit behavior trees, especially for complex multi-branch trees. Use manual JSON editing only for quick tweaks or automated pipelines.</div><h2 id=\"bt-events\">Behavior Tree Events</h2><p>Behavior tree nodes fire events that your components can listen to. These events follow the pattern <code>ai.{nodeID}.onStart</code> and <code>ai.{nodeID}.onEnd</code>:</p><div class=\"table-wrapper\"><table><thead><tr><th>Event</th><th>Description</th></tr></thead><tbody><tr><td><code>ai.{nodeID}.onStart</code></td><td>Fired when a specific BT node begins execution</td></tr><tr><td><code>ai.{nodeID}.onEnd</code></td><td>Fired when a specific BT node finishes (success or failure)</td></tr></tbody></table></div><div class=\"code-header\"><span>Lua</span></div><pre><code>-- Listen for when the \"chase\" node starts\nentity.on(\"ai.chase.onStart\", function()\n  entity.emit(\"entity.animate\", { animation = \"run\" })\nend)\n\n-- Listen for when the \"chase\" node ends\nentity.on(\"ai.chase.onEnd\", function()\n  entity.emit(\"entity.animate\", { animation = \"idle\" })\nend)</code></pre><p>These events are useful for triggering animations, sound effects, or other visual feedback that should sync with AI state transitions.</p><h2 id=\"complete-example\">Complete Example: Patrol-and-Chase Behavior Tree</h2><p>Here is a full behavior tree file for an enemy that patrols an area, chases the player when they get close, attacks when in range, and retreats when health is low:</p><div class=\"code-header\"><span>JSON</span></div><pre><code>{\n  \"root\": {\n    \"type\": \"selector\",\n    \"children\": {\n      \"retreat\": {\n        \"type\": \"sequence\",\n        \"commands\": [\n          {\n            \"name\": \"ai.checkHealth\",\n            \"parameters\": {\n              \"below\": 0.2\n            }\n          }\n        ],\n        \"children\": {\n          \"flee\": {\n            \"type\": \"action\",\n            \"commands\": [\n              {\n                \"name\": \"ai.flee\",\n                \"parameters\": {\n                  \"target\": \"player\",\n                  \"speed\": 90,\n                  \"distance\": 150\n                }\n              }\n            ]\n          }\n        }\n      },\n      \"combat\": {\n        \"type\": \"sequence\",\n        \"commands\": [\n          {\n            \"name\": \"ai.checkDistance\",\n            \"parameters\": {\n              \"target\": \"player\",\n              \"maxDistance\": 120\n            }\n          }\n        ],\n        \"children\": {\n          \"engage\": {\n            \"type\": \"selector\",\n            \"children\": {\n              \"attack\": {\n                \"type\": \"sequence\",\n                \"commands\": [\n                  {\n                    \"name\": \"ai.checkDistance\",\n                    \"parameters\": {\n                      \"target\": \"player\",\n                      \"maxDistance\": 28\n                    }\n                  },\n                  {\n                    \"name\": \"ai.faceTarget\",\n                    \"parameters\": { \"target\": \"player\" }\n                  },\n                  {\n                    \"name\": \"ai.attack\",\n                    \"parameters\": {}\n                  },\n                  {\n                    \"name\": \"ai.wait\",\n                    \"parameters\": { \"duration\": 0.8 }\n                  }\n                ]\n              },\n              \"chase\": {\n                \"type\": \"action\",\n                \"commands\": [\n                  {\n                    \"name\": \"ai.moveTo\",\n                    \"parameters\": {\n                      \"target\": \"player\",\n                      \"speed\": 70\n                    }\n                  }\n                ]\n              }\n            }\n          }\n        }\n      },\n      \"idle\": {\n        \"type\": \"action\",\n        \"commands\": [\n          {\n            \"name\": \"ai.patrol\",\n            \"parameters\": {\n              \"radius\": 50,\n              \"waitTime\": 2.5,\n              \"speed\": 30\n            }\n          }\n        ]\n      }\n    }\n  }\n}</code></pre><p>This behavior tree works as follows:</p><ol><li><strong>Retreat check</strong> (highest priority) -- if health drops below 20%, the entity flees from the player.</li><li><strong>Combat</strong> -- if the player is within 120 pixels, the entity enters combat mode. Within combat, it first checks if the player is close enough to attack (28 pixels). If so, it faces the target, attacks, and waits for a cooldown. If not in attack range, it chases the player.</li><li><strong>Idle patrol</strong> (lowest priority) -- if no other condition is met, the entity patrols randomly within a 50-pixel radius.</li></ol><p>Because the root is a <code>selector</code>, the tree evaluates these branches from top to bottom and executes the first one that succeeds. This creates a natural priority system where retreat overrides combat, and combat overrides patrol.</p><div class=\"callout info\"><div class=\"callout-title\">Next Steps</div>See the <a href=\"/guides/modding-guide\">Modding Guide</a> for practical tutorials on creating complete game content using entities, components, and behavior trees together.</div>"
}
