{
  "title": "Modding Guide",
  "slug": "guides/modding-guide",
  "section": "Guides",
  "subsection": null,
  "order": 1,
  "navTitle": "Modding Guide",
  "description": "A comprehensive guide for creating mods in Blastback Engine, covering custom entities, weapons, UI elements, and best practices.",
  "keywords": ["modding", "mods", "custom entities", "weapons", "UI", "best practices", "WeaponModel", "ShooterComponent", "MeleeComponent"],
  "showToc": true,
  "content": "<h1 id=\"modding-guide\">Modding Guide</h1><p class=\"page-subtitle\">A practical guide to creating custom content for Blastback Engine. Learn how to build entities, weapons, UI elements, and more.</p><div class=\"callout info\"><div class=\"callout-title\">Prerequisites</div>Before diving in, make sure you have read the <a href=\"getting-started.html\">Getting Started</a> guide and understand the basics of the <a href=\"entity-scripting.html\">Entity Scripting</a> system.</div><h2 id=\"creating-custom-entities\">Creating Custom Entities</h2><p>Modding in Blastback Engine revolves around creating JSON entity files and composing them from existing components. To create a custom entity:</p><ol><li><strong>Create a new JSON file</strong> in the <code>Content/Entities/</code> directory with a descriptive name (e.g., <code>fire_bat.mob.json</code>).</li><li><strong>Define the base properties</strong> including name, visuals path, render layer, and physics configuration.</li><li><strong>Add components</strong> to give the entity its behavior -- movement, animation, AI, health, and weapons.</li><li><strong>Create a behavior tree</strong> if the entity needs AI (place it in <code>Content/BehaviorTrees/</code> as <code>bt.[ID].json</code>).</li><li><strong>Add sprite assets</strong> to <code>Content/Sprites/</code> in PNG, ASE, or Atlas format.</li></ol><h3 id=\"naming-convention\">File Naming Convention</h3><div class=\"table-wrapper\"><table><thead><tr><th>Pattern</th><th>Model Type</th><th>Example</th></tr></thead><tbody><tr><td><code>[name].mob.json</code></td><td>Mob (enemies, NPCs)</td><td><code>goblin.mob.json</code></td></tr><tr><td><code>[name].weapon.json</code></td><td>Weapon</td><td><code>laser_rifle.weapon.json</code></td></tr><tr><td><code>[name].projectile.json</code></td><td>Projectile</td><td><code>fireball.projectile.json</code></td></tr><tr><td><code>[name].drop.json</code></td><td>Item drop</td><td><code>health_potion.drop.json</code></td></tr></tbody></table></div><div class=\"code-header\"><span>fire_bat.mob.json</span></div><pre><code>{\n  \"name\": \"Fire Bat\",\n  \"visualsPath\": \"enemies/fire_bat\",\n  \"renderLayer\": 5,\n  \"colliderSizeX\": 10,\n  \"colliderSizeY\": 10,\n  \"physicsLayer\": 3,\n  \"collisionLayerMask\": [1, 2, 4],\n  \"components\": {\n    \"animator\": {\n      \"triggers\": {\n        \"idle\": {},\n        \"fly\": {},\n        \"attack\": { \"hit\": 3 }\n      }\n    },\n    \"ai\": {\n      \"templateID\": \"flying_chase\"\n    },\n    \"mob\": {\n      \"health\": 20,\n      \"damage\": 8\n    },\n    \"movement\": {\n      \"speed\": 85.0\n    },\n    \"levitation\": {\n      \"amplitude\": 3.0,\n      \"frequency\": 2.0\n    }\n  },\n  \"model\": {\n    \"id\": \"fire_bat\",\n    \"namespc\": \"enemies\"\n  }\n}</code></pre><div class=\"callout tip\"><div class=\"callout-title\">Tip</div>Start by duplicating an existing entity file that is similar to what you want to create, then modify the properties and components. This is faster than building from scratch and helps avoid missing required fields.</div><h2 id=\"component-system\">Working with the Component System</h2><p>Components are added to the <code>components</code> object in your entity JSON. Each key is the component type identifier, and the value is its configuration.</p><h3>Commonly Used Components</h3><div class=\"table-wrapper\"><table><thead><tr><th>Component</th><th>Purpose</th><th>Typical Use</th></tr></thead><tbody><tr><td><code>animator</code></td><td>Sprite animation control</td><td>All visual entities</td></tr><tr><td><code>movement</code></td><td>Physics-based movement</td><td>Characters, enemies, NPCs</td></tr><tr><td><code>steeringMovement</code></td><td>Avoidance and flocking</td><td>Groups of enemies</td></tr><tr><td><code>mob</code></td><td>Health, damage, knockback</td><td>Enemies and destructible props</td></tr><tr><td><code>ai</code></td><td>Behavior tree connection</td><td>Autonomous entities</td></tr><tr><td><code>weaponHandler</code></td><td>Weapon equip and use</td><td>Armed enemies and players</td></tr><tr><td><code>trigger</code></td><td>Event zone detection</td><td>Interactive areas, traps</td></tr><tr><td><code>spawner</code></td><td>Entity spawning</td><td>Spawn points, generators</td></tr><tr><td><code>levitation</code></td><td>Floating hover effect</td><td>Flying enemies, floating items</td></tr><tr><td><code>input</code></td><td>Player input handling</td><td>Player entity only</td></tr></tbody></table></div><h3>Combining Components with commandMap</h3><p>The power of the component system comes from composition. The <code>commandMap</code> system ensures components communicate through events rather than direct references, so any combination is valid.</p><div class=\"code-header\"><span>Component combination example</span></div><pre><code>\"components\": {\n  \"animator\": { ... },\n  \"movement\": { \"speed\": 50.0 },\n  \"mob\": { \"health\": 100, \"damage\": 0 },\n  \"trigger\": {\n    \"commandMap\": {\n      \"onPlayerEnter\": \"entity.animate open,once\",\n      \"onPlayerExit\": \"entity.animate close,once\"\n    },\n    \"events\": {\n      \"onCollision\": [\"onPlayerEnter\"],\n      \"onCollisionExit\": [\"onPlayerExit\"]\n    }\n  },\n  \"interactionHandler\": {\n    \"interactionKey\": \"E\",\n    \"commandMap\": {\n      \"onInteract\": \"entity.emit activate\"\n    }\n  }\n}</code></pre><h2 id=\"custom-weapons\">Creating Custom Weapons</h2><p>Weapons in Blastback Engine are defined through a combination of models and components. The three key pieces are <strong>WeaponModel</strong>, <strong>ShooterComponent</strong>, and <strong>MeleeComponent</strong>.</p><h3 id=\"weapon-model\">WeaponModel</h3><p>The <code>WeaponModel</code> defines the weapon's core data -- its name, damage, fire rate, visual representation, and projectile configuration.</p><div class=\"code-header\"><span>plasma_cannon.weapon.json</span></div><pre><code>{\n  \"name\": \"Plasma Cannon\",\n  \"visualsPath\": \"weapons/plasma_cannon\",\n  \"renderLayer\": 4,\n  \"components\": {\n    \"weaponComponent\": {\n      \"damage\": 20,\n      \"knockback\": 80,\n      \"type\": \"ranged\"\n    },\n    \"shooterComponent\": {\n      \"projectileEntity\": \"plasma_bolt\",\n      \"fireRate\": 0.4,\n      \"spread\": 3.0,\n      \"burstCount\": 1,\n      \"muzzleOffset\": 8.0\n    },\n    \"animator\": {\n      \"triggers\": {\n        \"fire\": { \"muzzleFlash\": 1 }\n      }\n    }\n  },\n  \"model\": {\n    \"cooldown\": 0.4,\n    \"maxAmmo\": 30,\n    \"visual\": {\n      \"sprite\": \"plasma_cannon\",\n      \"scale\": 1.0\n    }\n  }\n}</code></pre><h3 id=\"shooter-component\">ShooterComponent</h3><p>For ranged weapons, the <code>ShooterComponent</code> handles projectile spawning, aiming, recoil, and ammunition.</p><div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>projectileEntity</code></td><td>string</td><td>Entity ID of the projectile to spawn</td></tr><tr><td><code>fireRate</code></td><td>float</td><td>Seconds between shots</td></tr><tr><td><code>spread</code></td><td>float</td><td>Angular spread in degrees for shot randomization</td></tr><tr><td><code>burstCount</code></td><td>int</td><td>Number of projectiles per burst (1 for single shot)</td></tr><tr><td><code>burstDelay</code></td><td>float</td><td>Delay between burst projectiles</td></tr><tr><td><code>muzzleOffset</code></td><td>float</td><td>Spawn offset for projectiles relative to weapon origin</td></tr></tbody></table></div><h3 id=\"melee-component\">MeleeComponent</h3><p>For melee weapons, the <code>MeleeComponent</code> handles swing arcs, hit detection, and damage application. Melee attacks use the Melee Impacts physics layer (8) for hit detection.</p><div class=\"code-header\"><span>battle_axe.weapon.json</span></div><pre><code>{\n  \"name\": \"Battle Axe\",\n  \"visualsPath\": \"weapons/battle_axe\",\n  \"renderLayer\": 4,\n  \"components\": {\n    \"weaponComponent\": {\n      \"damage\": 35,\n      \"knockback\": 150,\n      \"type\": \"melee\"\n    },\n    \"meleeComponent\": {},\n    \"animator\": {\n      \"triggers\": {\n        \"swing\": { \"impact\": 4 }\n      }\n    }\n  },\n  \"model\": {\n    \"cooldown\": 0.8,\n    \"maxAmmo\": -1,\n    \"visual\": {\n      \"sprite\": \"battle_axe\",\n      \"scale\": 1.2\n    }\n  }\n}</code></pre><div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>damage</code></td><td>int</td><td>Base damage per hit</td></tr><tr><td><code>knockback</code></td><td>float</td><td>Knockback force applied on hit</td></tr><tr><td><code>range</code></td><td>float</td><td>Reach distance of the melee attack</td></tr><tr><td><code>arc</code></td><td>float</td><td>Swing arc in degrees</td></tr><tr><td><code>attackDuration</code></td><td>float</td><td>Duration of the attack animation window</td></tr><tr><td><code>cooldown</code></td><td>float</td><td>Time before the weapon can attack again</td></tr></tbody></table></div><h2 id=\"custom-projectiles\">Creating Projectiles</h2><p>Projectiles are lightweight entities that travel in a direction and apply effects on collision.</p><div class=\"code-header\"><span>plasma_bolt.projectile.json</span></div><pre><code>{\n  \"name\": \"Plasma Bolt\",\n  \"visualsPath\": \"projectiles/plasma_bolt\",\n  \"renderLayer\": 3,\n  \"colliderSizeX\": 6,\n  \"colliderSizeY\": 6,\n  \"isTrigger\": true,\n  \"physicsLayer\": 4,\n  \"collisionLayerMask\": [1, 3],\n  \"model\": {\n    \"speed\": 200.0,\n    \"damage\": 20,\n    \"lifetime\": 3.0,\n    \"piercing\": false,\n    \"gravity\": 0.0\n  }\n}</code></pre><h2 id=\"ui-elements\">Creating UI Elements</h2><p>The engine provides 12 UI components for building in-game interfaces. UI elements are entities with specialized components that render on the screen layer rather than the world layer.</p><h3>Available UI Components</h3><div class=\"table-wrapper\"><table><thead><tr><th>Component</th><th>Purpose</th></tr></thead><tbody><tr><td><code>uiPanel</code></td><td>Background panel with configurable size, color, and opacity</td></tr><tr><td><code>uiLabel</code></td><td>Text display with font, size, color, and alignment options</td></tr><tr><td><code>uiImage</code></td><td>Static or animated image display</td></tr><tr><td><code>uiButton</code></td><td>Interactive button with hover, press, and click events</td></tr><tr><td><code>uiProgressBar</code></td><td>Horizontal or vertical fill bar for health, loading, etc.</td></tr><tr><td><code>uiSlider</code></td><td>Draggable slider for numeric value input</td></tr><tr><td><code>uiToggle</code></td><td>On/off toggle switch</td></tr><tr><td><code>uiList</code></td><td>Scrollable list of items</td></tr><tr><td><code>uiGrid</code></td><td>Grid layout container for inventory-style displays</td></tr><tr><td><code>uiTooltip</code></td><td>Context-sensitive popup text on hover</td></tr><tr><td><code>uiDialog</code></td><td>Dialog box with text, portrait, and response options</td></tr><tr><td><code>uiMinimap</code></td><td>Miniature map view with entity tracking</td></tr></tbody></table></div><div class=\"code-header\"><span>UI health bar example</span></div><pre><code>{\n  \"name\": \"PlayerHealthBar\",\n  \"visualsPath\": \"ui/health_bar\",\n  \"renderLayer\": 10,\n  \"components\": {\n    \"uiPanel\": {\n      \"width\": 120,\n      \"height\": 16,\n      \"color\": \"#1a1a2e\",\n      \"opacity\": 0.8\n    },\n    \"uiProgressBar\": {\n      \"width\": 116,\n      \"height\": 12,\n      \"fillColor\": \"#e74c3c\",\n      \"backgroundColor\": \"#2c3e50\",\n      \"value\": 1.0,\n      \"bindTo\": \"player.health\"\n    }\n  }\n}</code></pre><div class=\"callout info\"><div class=\"callout-title\">Note</div>UI entities use the same component system as game entities. You can combine UI components with event handling to create interactive menus, HUDs, and dialogs.</div><h2 id=\"tips-best-practices\">Tips and Best Practices</h2><h3>Entity Design</h3><ul><li><strong>Keep entities focused.</strong> Each entity should represent one logical game object. Avoid putting unrelated behaviors on the same entity.</li><li><strong>Use descriptive names.</strong> Entity names like <code>fire_bat</code> or <code>health_potion_drop</code> are easier to work with than <code>entity_001</code>.</li><li><strong>Set appropriate physics layers.</strong> Only include layers in <code>collisionLayerMask</code> that the entity actually needs to interact with. Fewer collision checks means better performance.</li><li><strong>Start simple.</strong> Begin with the minimum set of components and add complexity incrementally. Test after each change.</li></ul><h3>Performance</h3><ul><li><strong>Use <code>entity.onVisible</code></strong> to defer expensive operations until an entity is on screen.</li><li><strong>Limit AI complexity</strong> for entities that appear in large numbers. Simple patrol trees are cheaper than complex multi-branch decision trees.</li><li><strong>Reuse behavior tree templates</strong> with <code>templateID</code> instead of duplicating tree definitions. The engine caches resolved templates.</li><li><strong>Prefer smaller collision radii</strong> when precise collision is not critical. Larger colliders require more collision checks.</li></ul><h3>Modding Workflow</h3><ul><li><strong>Copy and modify</strong> existing entities rather than building from scratch. This ensures you do not miss required fields.</li><li><strong>Use the AI editor</strong> at <a href=\"https://jaschez.github.io/blastback/ai-editor/\">jaschez.github.io/blastback/ai-editor</a> for behavior trees instead of hand-editing JSON.</li><li><strong>Test incrementally.</strong> Add one component at a time and verify each works before adding the next.</li><li><strong>Check the console</strong> for error messages if an entity does not behave as expected. The engine logs missing components, invalid parameters, and broken references.</li></ul><h2 id=\"common-pitfalls\">Common Pitfalls</h2><div class=\"callout warning\"><div class=\"callout-title\">Watch Out For These</div><p>These are the most frequent issues encountered when modding Blastback Engine:</p></div><h3>1. Missing or Mismatched Physics Layers</h3><p>If your entity does not collide with anything, check that <code>physicsLayer</code> and <code>collisionLayerMask</code> are set correctly. Remember that collision is bidirectional -- both entities must include each other's layer in their mask.</p><h3>2. Behavior Tree File Naming</h3><p>BT files <strong>must</strong> be named <code>bt.[ID].json</code>. If you name a file <code>slime_patrol.json</code> instead of <code>bt.slime_patrol.json</code>, the engine will not recognize it as a behavior tree.</p><h3>3. Invalid Visual Paths</h3><p>The <code>visualsPath</code> must point to an existing asset in the <code>Content/Sprites/</code> directory. Omit the file extension -- the engine resolves the correct format automatically. Use forward slashes for path separators.</p><h3>4. Circular Template References</h3><p>If behavior tree template A references template B, and template B references template A, the resolution system will fail. Always ensure template references form a directed acyclic graph.</p><h3>5. Forgetting the commandMap</h3><p>If you add a component that should respond to events but nothing happens, verify that the <code>commandMap</code> is properly configured. Commands are only routed to components that explicitly declare handlers for them.</p><h3>6. Overlapping Collider Sizes</h3><p>If colliders are too large relative to the sprite, entities will appear to collide before they visually touch. Match <code>colliderSizeX</code> and <code>colliderSizeY</code> to the sprite's actual hit area, not the full image bounds.</p><h3>7. Animation Frame Mismatches</h3><p>Trigger frame numbers in the <code>animator</code> component must match actual frames in the <code>.ase</code> file. If a trigger references frame 8 but the animation only has 6 frames, the event will never fire.</p><div class=\"callout info\"><div class=\"callout-title\">Need Help?</div>Refer to the <a href=\"/reference/components-overview\">Components Reference</a> for detailed documentation on every component's parameters, commands, and events. The <a href=\"/reference/models-overview\">Models Reference</a> covers all data model schemas.</div>"
}
