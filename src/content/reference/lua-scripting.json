{
  "title": "Lua Scripting API",
  "slug": "reference/lua-scripting",
  "section": "Core Concepts",
  "order": 5,
  "navTitle": "Lua Scripting",
  "description": "Complete reference for the Blastback Engine Lua scripting API — the ctx object, engine globals, timers, buffers, commands, and lifecycle hooks.",
  "keywords": ["lua", "scripting", "moonsharp", "ctx", "context", "commands", "events", "timers", "buffers", "input", "api"],
  "content": "<h1 id=\"lua-scripting-api\">Lua Scripting API</h1>\n<p class=\"page-subtitle\">Complete reference for writing Lua scripts in the Blastback Engine. Scripts interact with entities through the <code>ctx</code> context object and <code>engine</code> globals.</p>\n<span class=\"tag tag-function\">Lua</span> <span class=\"tag tag-component\">MoonSharp</span>\n\n<div class=\"callout info\"><div class=\"callout-title\">Runtime</div>Blastback uses <strong>MoonSharp</strong> (a .NET Lua interpreter) in soft sandbox mode. Each script runs in its own isolated environment &mdash; no state leaks between scripts.</div>\n\n<h2 id=\"overview\">Overview</h2>\n<p>Lua scripts in Blastback are components that attach to entities via JSON definitions. Scripts can:</p>\n<ul>\n<li>React to input and execute entity commands</li>\n<li>Manage timed behaviors with timers and input buffers</li>\n<li>Store and expose stateful properties to other components</li>\n<li>Integrate with the entity command resolution system</li>\n</ul>\n\n<h2 id=\"json-schema\">Defining Lua Components in JSON</h2>\n<p>Lua components are defined in entity JSON files using the <code>lua:</code> prefix. All JSON properties become available in the script via <code>ctx.config</code>.</p>\n\n<div class=\"code-header\"><span>Entity JSON</span></div>\n<pre><code>{\n  \"components\": {\n    \"lua:combat_momentum\": {\n      \"maxTier\": 5,\n      \"speedPerTier\": 8,\n      \"comboTimeout\": 1.5,\n      \"decayInterval\": 0.4\n    }\n  }\n}</code></pre>\n\n<div class=\"callout tip\"><div class=\"callout-title\">Naming</div>The component name <code>lua:script_id</code> maps to a Lua script file loaded with the matching ID. Script files are located in <code>Content/Definition/Data/Game/scripts/</code>.</div>\n\n<hr>\n\n<h2 id=\"lifecycle\">Script Lifecycle Functions</h2>\n<p>Scripts define these functions to hook into the component lifecycle:</p>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">init(ctx, config)</code><span class=\"tag tag-function\">Lifecycle</span></div><div class=\"api-block-body\"><p>Called once when the component is added to the entity. Use this to initialize state, timers, and buffers.</p>\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td>ctx</td><td>LuaBridgeContext</td><td>The context object for interacting with the entity.</td></tr><tr><td>config</td><td>Table</td><td>Lua table containing all JSON configuration values.</td></tr></tbody></table></div></div></div>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">onAddedToEntity(ctx)</code><span class=\"tag tag-function\">Lifecycle</span></div><div class=\"api-block-body\"><p>Called after <code>init</code>, when the component is fully added to the entity. Optional hook for setup that needs the context fully initialized.</p></div></div>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">update(ctx, dt)</code><span class=\"tag tag-function\">Lifecycle</span></div><div class=\"api-block-body\"><p>Called every frame. Only invoked if the function is defined in the script.</p>\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td>ctx</td><td>LuaBridgeContext</td><td>The context object.</td></tr><tr><td>dt</td><td>float</td><td>Delta time for the current frame.</td></tr></tbody></table></div></div></div>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">commands()</code><span class=\"tag tag-function\">Lifecycle</span></div><div class=\"api-block-body\"><p>Returns a table of command handler functions. Each function is registered as an entity command and should return <code>bool</code> (true = success).</p>\n<div class=\"code-header\"><span>Lua</span></div>\n<pre><code>function commands()\n  return {\n    onHit = function()\n      tier = tier + 1\n      return true\n    end,\n    onBreak = function()\n      tier = 0\n      return true\n    end\n  }\nend</code></pre></div></div>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">properties()</code><span class=\"tag tag-function\">Lifecycle</span></div><div class=\"api-block-body\"><p>Returns a table of property getter functions. Each function returns a <code>float</code> and is registered as a PropertyReference emitter. Values are polled when <code>ctx.notify()</code> is called.</p>\n<div class=\"code-header\"><span>Lua</span></div>\n<pre><code>function properties()\n  return {\n    getTier = function() return tier end,\n    getBonus = function() return tier * speedPerTier end\n  }\nend</code></pre></div></div>\n\n<hr>\n\n<h2 id=\"ctx-object\">The <code>ctx</code> Object</h2>\n<p>The <code>ctx</code> parameter (LuaBridgeContext) is the primary interface between Lua scripts and the engine.</p>\n\n<h3 id=\"ctx-commands\">Command Execution</h3>\n<p>Commands are resolved through the entity's command system. All return <code>bool</code> indicating success/failure. Commands are cached by path after first resolution for performance.</p>\n\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Method</th><th>Parameters</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>ctx.command(path)</td><td>string</td><td>Execute a parameterless command.</td></tr>\n<tr><td>ctx.commandS(path, param)</td><td>string, string</td><td>Command with a string parameter.</td></tr>\n<tr><td>ctx.commandF(path, param)</td><td>string, float</td><td>Command with a float parameter.</td></tr>\n<tr><td>ctx.commandFF(path, p1, p2)</td><td>string, float, float</td><td>Command with two float parameters.</td></tr>\n<tr><td>ctx.commandV(path, x, y)</td><td>string, float, float</td><td>Command with a Vector2 (passed as separate x, y).</td></tr>\n<tr><td>ctx.commandB(path, param)</td><td>string, bool</td><td>Command with a boolean parameter.</td></tr>\n</tbody>\n</table></div>\n\n<div class=\"callout info\"><div class=\"callout-title\">Vector2 Note</div>Vector2 values are passed as separate <code>x</code> and <code>y</code> floats due to MoonSharp interop limitations. The engine converts them internally.</div>\n\n<h3 id=\"ctx-queries\">Queries</h3>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">ctx.queryFloat(path)</code><span class=\"tag tag-function\">Query</span></div><div class=\"api-block-body\"><p>Query a float value from a command path. Always returns a float (converted from Lua number).</p>\n<div class=\"code-header\"><span>Lua</span></div>\n<pre><code>local angle = ctx.queryFloat(\"aimAssist.getCorrectedAngle\")\nlocal interval = ctx.queryFloat(\"state.refillInterval\")</code></pre></div></div>\n\n<h3 id=\"ctx-events\">Events &amp; Notifications</h3>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">ctx.emit(eventID)</code><span class=\"tag tag-event\">Event</span></div><div class=\"api-block-body\"><p>Emit an entity event. All components listening for this event ID will be notified.</p>\n<div class=\"code-header\"><span>Lua</span></div>\n<pre><code>ctx.emit(\"onDamaged\")\nctx.emit(\"alert\")</code></pre></div></div>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">ctx.notify()</code><span class=\"tag tag-event\">Event</span></div><div class=\"api-block-body\"><p>Notify the system that Lua-side property values have changed, triggering PropertyReference emitters to re-poll values.</p></div></div>\n\n<h3 id=\"ctx-config\">Configuration</h3>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">ctx.config</code><span class=\"tag tag-component\">Property</span></div><div class=\"api-block-body\"><p>Lua table containing all JSON configuration values from the component definition. Access values as <code>ctx.config.key</code> or through the <code>config</code> parameter in <code>init()</code>.</p></div></div>\n\n<h3 id=\"ctx-input\">Input</h3>\n<p>Access player input via <code>ctx.input</code>:</p>\n\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Property / Method</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>ctx.input.aimX</td><td>float</td><td>X component of the aim direction.</td></tr>\n<tr><td>ctx.input.aimY</td><td>float</td><td>Y component of the aim direction.</td></tr>\n<tr><td>ctx.input.moveX</td><td>float</td><td>X component of movement direction.</td></tr>\n<tr><td>ctx.input.moveY</td><td>float</td><td>Y component of movement direction.</td></tr>\n<tr><td>ctx.input.flipState</td><td>bool</td><td>Current flip/facing state of the entity.</td></tr>\n<tr><td>ctx.input.check(key)</td><td>bool</td><td>Check if an action key is currently pressed.</td></tr>\n</tbody>\n</table></div>\n\n<hr>\n\n<h2 id=\"engine-globals\">The <code>engine</code> Global Namespace</h2>\n<p>Global engine utilities available in all scripts.</p>\n\n<h3 id=\"timers\">Timers</h3>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">engine.timer(interval, options?)</code><span class=\"tag tag-function\">Factory</span></div><div class=\"api-block-body\"><p>Create a timer that expires after the given interval in seconds.</p>\n<div class=\"table-wrapper\"><table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td>interval</td><td>float</td><td>Time in seconds until expiration.</td></tr>\n<tr><td>options</td><td>table (optional)</td><td><code>{ repeating = true }</code> to auto-reset on expiry.</td></tr>\n</tbody></table></div>\n<p><strong>Timer methods:</strong></p>\n<div class=\"table-wrapper\"><table><thead><tr><th>Method</th><th>Returns</th><th>Description</th></tr></thead><tbody>\n<tr><td>expired()</td><td>bool</td><td>True if timer has expired. If repeating, resets automatically.</td></tr>\n<tr><td>reset()</td><td>void</td><td>Reset the timer to expire after the interval again.</td></tr>\n</tbody></table></div></div></div>\n\n<h3 id=\"buffers\">Input Buffers</h3>\n\n<div class=\"api-block\"><div class=\"api-block-header\"><code class=\"api-block-name\">engine.buffer()</code><span class=\"tag tag-function\">Factory</span></div><div class=\"api-block-body\"><p>Create an input buffer for storing and consuming buffered actions.</p>\n<p><strong>Buffer methods:</strong></p>\n<div class=\"table-wrapper\"><table><thead><tr><th>Method</th><th>Returns</th><th>Description</th></tr></thead><tbody>\n<tr><td>set()</td><td>void</td><td>Mark the buffer as active.</td></tr>\n<tr><td>clear()</td><td>void</td><td>Clear the buffered state.</td></tr>\n<tr><td>isSet()</td><td>bool</td><td>Check if the buffer is currently set.</td></tr>\n<tr><td>consume(condition)</td><td>bool</td><td>If buffered AND condition() returns true, clears buffer and returns true.</td></tr>\n</tbody></table></div></div></div>\n\n<h3 id=\"time\">Time</h3>\n\n<div class=\"table-wrapper\"><table>\n<thead><tr><th>Function</th><th>Returns</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>engine.deltaTime()</td><td>float</td><td>Delta time for the current frame.</td></tr>\n<tr><td>engine.totalTime()</td><td>float</td><td>Total elapsed time since engine startup.</td></tr>\n</tbody>\n</table></div>\n\n<hr>\n\n<h2 id=\"integration\">Integration with State Machines &amp; Action Resolvers</h2>\n\n<h3 id=\"state-machines\">State Machine Commands</h3>\n<p>Lua functions can be referenced in state machine definitions using the <code>lua:</code> prefix:</p>\n<div class=\"code-header\"><span>JSON</span></div>\n<pre><code>{\n  \"states\": {\n    \"idle\": {\n      \"onEnter\": [\"lua:my_script.onEnterIdle\"],\n      \"update\": [\"lua:my_script.idleUpdate\"],\n      \"transitions\": [\n        { \"check\": \"action\", \"commands\": [\"lua:my_script.checkTransition\"] }\n      ]\n    }\n  },\n  \"globalUpdate\": [\"lua:my_script.update\"]\n}</code></pre>\n\n<h3 id=\"action-resolvers\">Action Resolvers</h3>\n<p>In control handler definitions, Lua functions can resolve input actions:</p>\n<div class=\"code-header\"><span>JSON</span></div>\n<pre><code>{\n  \"controlHandler\": {\n    \"actionMap\": {\n      \"aim\": { \"resolver\": \"lua:action_resolvers.aim\" },\n      \"move\": { \"resolver\": \"lua:action_resolvers.move\" },\n      \"toss\": { \"resolver\": \"lua:action_resolvers.toss\" }\n    }\n  }\n}</code></pre>\n\n<hr>\n\n<h2 id=\"examples\">Complete Examples</h2>\n\n<h3 id=\"ex-action-resolver\">Action Resolver Script</h3>\n<div class=\"code-header\"><span>code.action_resolvers.lua</span></div>\n<pre><code>function aim(ctx)\n    ctx.commandV(\"weapon.aim\", ctx.input.aimX, ctx.input.aimY)\nend\n\nfunction move(ctx)\n    ctx.commandV(\"movement.move\", ctx.input.moveX, ctx.input.moveY)\nend\n\nfunction flip(ctx)\n    ctx.commandB(\"visual.flip\", ctx.input.flipState)\nend</code></pre>\n\n<h3 id=\"ex-stateful-component\">Stateful Component with Timers</h3>\n<div class=\"code-header\"><span>comp.combat_momentum.lua</span></div>\n<pre><code>local tier = 0\nlocal maxTier = 5\nlocal speedPerTier = 8\nlocal comboWindow = nil\nlocal decayTimer = nil\n\nfunction init(_ctx, config)\n    maxTier = config.maxTier or 5\n    speedPerTier = config.speedPerTier or 8\n    comboWindow = engine.timer(config.comboTimeout or 1.5)\n    decayTimer = engine.timer(config.decayInterval or 0.4, { repeating = true })\nend\n\nfunction update(ctx)\n    if comboWindow.expired() and tier > 0 then\n        if decayTimer.expired() then\n            tier = tier - 1\n            ctx.notify()\n        end\n    end\nend\n\nfunction commands()\n    return {\n        onHit = function()\n            tier = math.min(tier + 1, maxTier)\n            comboWindow.reset()\n            return true\n        end\n    }\nend\n\nfunction properties()\n    return {\n        getTier = function() return tier end,\n        getSpeedBonus = function() return tier * speedPerTier end\n    }\nend</code></pre>\n\n<h3 id=\"ex-buffered-input\">Buffered Input Pattern</h3>\n<div class=\"code-header\"><span>Buffered attack example</span></div>\n<pre><code>local attackBuffer = nil\n\nfunction init()\n    attackBuffer = engine.buffer()\nend\n\nfunction update(ctx)\n    if ctx.input.check(\"attack\") then\n        attackBuffer.set()\n    end\n\n    if attackBuffer.consume(function()\n        return ctx.command(\"weapon.canAttack\")\n    end) then\n        ctx.command(\"weapon.fire\")\n        ctx.emit(\"onAttack\")\n    end\nend</code></pre>\n\n<h3 id=\"ex-timer-refill\">Timer-based Refill</h3>\n<div class=\"code-header\"><span>code.player_refill.lua</span></div>\n<pre><code>local refillTimer = nil\n\nfunction update(ctx)\n    if refillTimer == nil then\n        local interval = ctx.queryFloat(\"state.refillInterval\")\n        refillTimer = engine.timer(interval, { repeating = true })\n    end\n\n    if refillTimer.expired() then\n        ctx.command(\"reloadAmmo\")\n    end\nend</code></pre>"
}
